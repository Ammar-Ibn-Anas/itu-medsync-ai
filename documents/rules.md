Naming: All Python variables/functions must be snake_case. All React components must be PascalCase.

Comments: Use natural, practical comments. Avoid "AI-perfect" over-explanation. Explain why a complex block exists, not what it does.

Error Handling: Always use try/except blocks for external API calls (Gemini/Supabase) to prevent the server from crashing during the demo.

Simplicity: If a feature takes more than 30 minutes to implement, we hardcode it or skip it. MVP first.